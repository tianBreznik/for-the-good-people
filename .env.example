# Firebase Admin — local dev: save JSON as firebase-service-account.json
FIREBASE_SERVICE_ACCOUNT_PATH=./firebase-service-account.json
# Production (Render etc.): paste minified JSON as one line — do not commit
# FIREBASE_SERVICE_ACCOUNT_JSON={"type":"service_account",...}

# Firestore: authorAllowlist (approved emails), authorApplications (pending requests)

# --- Email when someone submits an author application ---
ADMIN_NOTIFY_EMAIL=tian@perspective.credit

# SMTP (Gmail example: use an App Password, not your normal password)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=tian.dissertation@gmail.com
SMTP_PASS=Mosejskamiska1234!

# Optional
MAIL_FROM="Good People Posting <you@gmail.com>"
SITE_NAME=Good People Posting
